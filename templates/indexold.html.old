<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Lesson Planning AI</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --trust-green: #205c40;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8f9fa;
            margin: 0;
        }
        .sidebar {
            width: 200px;
            height: 100vh;
            background-color: var(--trust-green);
            padding-top: 2rem;
            position: fixed;
        }
        .sidebar a {
            display: block;
            padding: 12px 20px;
            color: #ffffff;
            text-decoration: none;
        }
        .sidebar a:hover, .sidebar a.active {
            background-color: #184b34;
            border-left: 4px solid #ffffff;
        }
        .main {
            margin-left: 200px;
            padding: 2rem;
        }
        .tool-card {
            border-radius: 12px;
            padding: 1rem;
            background: #fff;
            box-shadow: 0 2px 6px rgba(0,0,0,0.05);
            text-align: center;
            transition: transform 0.2s ease;
            position: relative;
            cursor: pointer;
            border: 2px solid var(--trust-green);
        }
        .tool-card:hover {
            transform: translateY(-5px);
        }
        .tool-card img {
            width: 80px;
            height: 80px;
            object-fit: contain;
            border-radius: 50%;
            margin-bottom: 1rem;
            border: 2px solid var(--trust-green);
            background-color: #fff;
        }
        .tool-card .badge {
            position: absolute;
            top: 10px;
            left: 10px;
            background-color: var(--trust-green);
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <a href="#" class="active"><i class="fas fa-home me-2"></i>Home</a>
        <a href="#"><i class="fas fa-star me-2"></i>Favourites</a>
        <a href="#"><i class="fas fa-toolbox me-2"></i>Tools</a>
        <a href="#"><i class="fas fa-comments me-2"></i>Chat</a>
        <a href="#"><i class="fas fa-users me-2"></i>Personas</a>
    </div>

    <div class="main">
        <div class="row g-4">
            <div class="col-md-4">
                <a href="/001" style="text-decoration: none; color: inherit;">
                    <div class="tool-card">
                        <span class="badge text-bg-light">New</span>
                        <img src="https://via.placeholder.com/80x80.png?text=LT" alt="Lesson Planning Logo">
                        <h5 class="fw-semibold mt-2 text-success">Lesson Planning Tool</h5>
                        <p class="text-muted small">Generate tailored lesson plans by entering subject and topic</p>
                    </div>
                </a>
            </div>
        </div>
    </div>
</body>
</html>
